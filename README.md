# Be-Ready 評価 POC アプリ

北海学園大学 佐藤教授との共同プロジェクト「Be-Ready人材育成プログラム」における評価軸の実証実験（POC）用 Web アプリ。

**コンセプト**: アンケート（自己評価）+ ログ（YWT）+ 振り返り の3要素で、Be-Ready人材の評価基準を体験・検証する。

---

## 目次

1. [プロダクト概要](#1-プロダクト概要)
2. [技術スタック・ファイル構成](#2-技術スタックファイル構成)
3. [データモデル](#3-データモデル)
4. [機能一覧](#4-機能一覧)
5. [デプロイ手順](#5-デプロイ手順)
6. [設計上の判断](#6-設計上の判断)
7. [今後の課題・拡張予定](#7-今後の課題拡張予定)
8. [関連システム](#8-関連システム)

---

## 1. プロダクト概要

**作成日**: 2026年5月26日  
**最終更新**: 2026年7月1日（0626ルーブリック実装・②⑧参考値表示）  
**バージョン**: v1.4（POC）  
**対象ユーザー**: 学生 / メンター

### 1.1 背景

座学教育との差別化を目的に、企業が「即戦力かどうか」を判断できる評価軸（9項目）を定義。その評価軸を実際に運用・検証するための POC ツールとして開発。

### 1.2 評価の3要素

| 要素 | 内容 | 評価への寄与 |
|---|---|---|
| アンケート | 9評価軸 × 記述式4択（`survey_questions.json` 定義）による自己判定 | 定量的スコア・成長可視化 |
| ログ | YWT法（やったこと / わかったこと / 次にやること）による振り返り | 思考プロセスの記録 |
| 振り返り | メンチメーター形式 / アンケート形式の2モード提出 | 成長実感の言語化・メンターへの提出 |

### 1.3 評価軸9項目（0626ルーブリック準拠）

| 番号 | 軸名 | 評価すること | 備考 |
|---|---|---|---|
| ① | 課題設定力 | 仕事について自分の視点からその価値・意味を理解しているか | 本採点 |
| ② | 情報活用力 | 情報を活用できるか | **参考値**（次元が異なる・基準未確定） |
| ③ | 不確実性への耐性 | 不確実性の高い仕事でも粘り強くかつ柔軟に取り組めるか | 本採点 |
| ④ | 提案・発信力 | やるべき・やりたいと考えることを他者に伝えられるか | 本採点 |
| ⑤ | 実行・改善力 | 他でもない自分のこととして粘り強く関われるか | 本採点 |
| ⑥ | オーナーシップ | やりたいと思っているか、意味を自分なりに理解できているか | 本採点 |
| ⑦ | 協働・調整力 | 他者の意見を取り入れつつ柔軟に対応できるか | 本採点 |
| ⑧ | 自律・内発的動機 | なぜやるかを自分で語れるか | **参考値**（理解レベルそのもの・基準未確定） |
| ⑨ | 行動変容力 | FBを行動の変化につなげられるか | 本採点 |

> **注意**: ②⑧は佐藤教授の概念整理により「次元が異なる」「省略の余地あり」とされている。0626ルーブリックでの詳細基準は未確定のため、アンケートでは現状参考値として扱う。

### 1.4 評価レベル（4分類・0626ルーブリック準拠）

| Lv | 名称 | 理解の種類 | 定義 |
|---|---|---|---|
| Lv.1 | 受動性 | 「知る」理解 | 対象を外から（外在的視点）見て、傍観者的・人ごと的にただ知っているだけの状態 |
| Lv.2 | 能動性 | 「分かる」理解（他者視点） | 他者の立場に立てば確かにそう思える、となっている状態 |
| Lv.3 | 自律性 | 「分かる」理解（自己視点） | 他者ではなく、自分の視点から見て確かにそう思える、となっている状態 |
| Lv.4 | 創造性 | 「発見」としての理解 | 教え手から学ぶのではなく、自らの行為・経験から新たにやりたい・やるべきことを見つけ出している |

### 1.5 佐藤教授による概念整理論点（2026/6/26）

次回ディスカッションで整理予定の未解決論点。アプリの評価軸の扱いに影響する可能性あり。

1. **②情報活用力の次元**: 他軸が「仕事の理解」を見るのに対し、②は「知識・情報の理解」を見ており次元が異なる
2. **⑧内発的動機の省略可能性**: ⑧は行動レベルでなく「理解レベルそのもの」であり、他項目も結局は内発的動機を測っているため省略の余地あり
3. **創造性の独立性**: 受動・能動・自律は「与えられた仕事の自分事化」だが、創造性は「自ら仕事を生み出す」→ 4つを一軸に並べる妥当性に留保
4. **ルーブリックとしての機能**: 次元の違いはあるが、記述してみると既にルーブリックとして機能する形になっている

---

## 2. 技術スタック・ファイル構成

### 2.1 技術スタック

| カテゴリ | 採用技術 | 補足 |
|---|---|---|
| 言語 | JavaScript (ES2022) | TypeScript 不採用（POC の軽量さ重視） |
| UI | React 18（関数コンポーネント + Hooks） | useState / useEffect / useMemo |
| スタイリング | インラインスタイル（ダークテーマ） | CSS変数不使用・カラー定数 `C` オブジェクトで管理 |
| グラフ | recharts | RadarChart（9軸レーダー） |
| アイコン | lucide-react | 軽量な SVG アイコンセット |
| データ永続化 | `localStorage`（ブラウザ標準） | 同期 API・個人スコープで保持 |
| ホスティング | Vercel + GitHub（予定） | `react-scripts` ベース |

### 2.2 ファイル構成

```
code/
├── src/
│   ├── App.jsx                  ← メインアプリ（全 UI・ロジックを集約）
│   └── index.js
├── public/
│   ├── index.html
│   └── survey_questions.json    ← アンケート設問定義（9軸 × 設問 × 選択肢）
├── be_ready_poc_app.jsx         ← 元バージョン（参照用・Tailwind + window.storage 構成）
├── be_ready_project_handover.md ← プロジェクト引き継ぎ資料
├── package.json
└── README.md
```

> **注意**: Node.js 14 以上が必要（`react-scripts 5` の要件）。v11 以下では `npm run build` が失敗する。

### 2.3 ファイルの役割分担

| ファイル | 役割 |
|---|---|
| `src/App.jsx` | 本体。学生・メンター両ロールの全 UI・ロジックを集約 |
| `public/survey_questions.json` | アンケート設問定義。軸ごとの質問文・選択肢・`axisWeights`（複数軸への寄与度）を定義 |
| `be_ready_poc_app.jsx` | 元バージョン（マージ参照用）。`window.storage` + Tailwind 構成 |

---

## 3. データモデル

### 3.1 ストレージキー設計

```
current_user                          // ログイン中ユーザー情報
students_list                         // 登録済み学生一覧（メンター向け）
survey:{userID}:{timestamp}           // 学生の自己評価アンケート
log:{userID}:{timestamp}              // YWT ログ
mentor_survey:{studentID}:{timestamp} // メンターによる学生評価
mjob:{corpId}                         // 求人分析結果キャッシュ（job-analyzer 連携時）
```

ポートフォリオはアンケートデータから動的に生成するため、専用ストレージキーなし。

### 3.2 ユーザー（current_user）

```js
{
  name: string,
  id:   string,
  role: 'student' | 'mentor',
}
```

### 3.3 学生リスト（students_list）

```js
[{ id: string, name: string, registeredAt: number }]
```

学生ロールでログインした際に自動登録。メンター画面の学生一覧に使用。

### 3.4 アンケート（survey）

```js
{
  userID:    string,
  timestamp: number,
  term:      string,   // 今期の目標
  axes: {
    1: number,   // Lv1〜4（calcAxesFromAnswers で survey_questions.json の axisWeights から算出）
    2: number,
    // ... 3〜9
  }
}
```

`axes` のキーは整数（`1`〜`9`）。値は `axisWeights` と回答値から加重平均で算出した 1〜4 の整数。

### 3.5 ログ（log）

```js
{
  userID:    string,
  timestamp: number,
  yatta:     string,         // Y: やったこと
  wakatta:   string,         // W: わかったこと
  tsugi:     string,         // T: 次にやること
  emotion:   1|2|3|4|5,     // 感情記録（1:😢 〜 5:😄）
}
```

### 3.6 メンター評価（mentor_survey）

```js
{
  mentorID:    string,
  mentorName:  string,
  studentID:   string,
  studentName: string,
  timestamp:   number,
  note:        string,   // 評価メモ（任意）
  axes: {
    1: number,   // Lv1〜4
    // ... 2〜9
  }
}
```

---

## 4. 機能一覧

### 4.1 ロール別画面構成

#### 学生（role: 'student'）

| 画面 | ナビ | 機能概要 |
|---|---|---|
| ホーム | ボトムナビ「ホーム」/ ヘッダー Be-Ready クリック | 統計カード + **Be-Ready 評価レーダーチャート**（アンケートがあれば表示）+ クイックアクション |
| アンケート入力 | ボトムナビ「アンケート」 | `survey_questions.json` から設問を fetch して表示。セクション別進捗バー。全問回答で保存ボタン表示 |
| ログ（YWT） | ボトムナビ「ログ」 | やったこと・わかったこと・次にやること + 感情記録。過去ログ一覧・削除 |
| ポートフォリオ | ボトムナビ「ポートフォリオ」 | アンケートから**自動生成**。今期目標・9軸レベルバー・強みのエリア（Lv3+）・成長のエリア（Lv1-2）・レーダーチャートを表示 |
| 振り返り | ボトムナビ「振り返り」 | サブタブ: **振り返り入力**（メンチメーター/アンケート形式）/ **問いへの回答**（メンターからの問い）/ **フィードバック** |

#### メンター（role: 'mentor'）

| 画面 | ナビ | 機能概要 |
|---|---|---|
| 学生一覧 | ボトムナビ「学生一覧」 | `students_list` から学生カード表示。平均Lv・アンケート件数 |
| 学生詳細 | 学生カードタップ | タブ切り替え（アンケート / ログ / 成果物）。自己評価とメンター評価を混在表示（バッジ区別） |
| 評価入力 | ボトムナビ「評価入力」 | 学生選択 → 9軸評価入力 → `mentor_survey` キーで保存。評価履歴表示 |

### 4.2 アンケート設問方式（survey_questions.json）

各設問は複数の評価軸への寄与度（`axisWeights`）を持つ。回答値（1〜4）と `axisWeights` の加重平均で軸スコアを算出（`calcAxesFromAnswers`）。

```json
{
  "sections": [
    {
      "title": "セクションタイトル",
      "questions": [
        {
          "id": "q1",
          "text": "設問文",
          "options": ["選択肢1", "選択肢2", "選択肢3", "選択肢4"],
          "axisWeights": { "1": 0.8, "3": 0.2 }
        }
      ]
    }
  ]
}
```

`survey_questions.json` が `public/` に存在しない場合、アンケート画面にエラーメッセージを表示（フォールバックなし）。

### 4.3 ②⑧「参考値」の UI 表示（v1.4 実装）

0626ルーブリックに基づき、②情報活用力・⑧自律・内発的動機は評価基準が未確定のため「参考値」として全画面で区別表示する。

| 画面 | 表示方法 |
|---|---|
| **アンケート回答履歴** | ②⑧のスコアタグをグレーアウト + 「※」マーク。下部に「参考値（基準未確定）」注記 |
| **ポートフォリオ 9軸バー** | ②⑧のカードを半透明化（`opacity: 0.7`）+ 「参考値」バッジ表示 |
| **ポートフォリオ 強み・成長エリア** | ②⑧を本採点（強み・成長）から除外。別セクション「参考値（評価基準未確定）」として独立表示 |
| **レーダーチャート（全画面）** | ②（情報）・⑧（動機）の軸ラベルに「※」マーク。チャート下部に「※ 情報（②）・動機（⑧）は基準未確定のため参考値」注記 |

`AXES` 定数の `ref: true` フラグで制御。基準確定後は `ref: false` に変更するだけで全画面が自動的に本採点扱いに切り替わる。

### 4.4 レーダーチャートの表示ロジック

recharts `RadarChart` で9評価軸を正多角形表示。

| 表示場所 | 表示条件 | 内容 |
|---|---|---|
| **ホーム画面** | アンケートが1件以上あるとき | 自己評価（紫 `#CC44FF`）。メンター評価があれば水色 `#00E5FF` で重ね表示 |
| **ポートフォリオ** | アンケートが1件以上あるとき | 同上 |
| **メンター評価画面** | 学生選択後・自己評価が1件以上あるとき | 同上 |

### 4.4 ポートフォリオ自動生成ロジック

アンケート保存時に動的生成（専用ストレージなし）。

| カード要素 | 内容 |
|---|---|
| 今期の目標 | アンケートで入力した `term` |
| 9軸レベルバー | 全軸スコアを色付きバーで一覧 |
| 強みのエリア | Lv3以上の軸 → 軸名 + 根拠となる選択肢テキスト |
| 成長のエリア | Lv1〜2の軸 → 軸名 + 根拠となる選択肢テキスト |
| レーダーチャート | 自己評価 vs メンター評価 |

---

## 5. デプロイ手順

### 前提

- Node.js 14 以上（`react-scripts 5` の要件）
- npm 6 以上

### Step 1: 依存関係のインストール

```bash
cd code
npm install
```

### Step 2: ローカル起動

```bash
npm start
```

ブラウザで `http://localhost:3000` を開く。

### Step 3: ビルド

```bash
npm run build
```

`build/` ディレクトリに静的ファイルが生成される。

### Step 4: Vercel へデプロイ（任意）

1. `build/` を GitHub にプッシュ
2. https://vercel.com/ でリポジトリをインポート
3. Build Command: `npm run build`、Output Directory: `build` を設定してデプロイ

---

## 6. 設計上の判断

### 6.1 単一ファイル構成（POC）

`src/App.jsx` 1ファイルに全コードを集約。POC 段階のため保守性より全体把握のしやすさを優先。AI（Claude）に全文渡してデバッグ・拡張しやすい構成。本番化時はコンポーネント分割を推奨。

### 6.2 `localStorage` の採用

元バージョン（`be_ready_poc_app.jsx`）の `window.storage`（Claude Artifacts 専用 API）を `localStorage` に変更。`react-scripts` ベースのローカル開発・Vercel デプロイに対応するため。`storage` ヘルパー関数に集約されており、将来的な DB 移行が容易。

### 6.3 アンケートを JSON 外部定義方式に

設問・選択肢・軸への寄与度（`axisWeights`）を `public/survey_questions.json` で管理。アプリを再ビルドせずに設問を更新でき、0626ルーブリックの改訂に迅速に対応できる。

### 6.4 ポートフォリオをアンケートから自動生成

手動入力ではなくアンケート回答から自動生成することで、重複入力を排除。強みのエリア・成長のエリアの表示により、自己認識を促す設計。

### 6.5 ②⑧の扱い（v1.4 実装済み）

佐藤教授の概念整理（§1.5）に基づき、②情報活用力と⑧内発的動機を「参考値」として扱う。`AXES` 定数に `ref: true` フラグを追加し、アンケート履歴・ポートフォリオ・レーダーチャートの全画面で区別表示を実装済み。基準確定後は `ref: false` に変更するだけで本採点扱いに切り替わる。

### 6.6 認証を省略

POC 段階では氏名・ID の入力のみで「ログイン」。セキュリティ実装はスコープ外。本番化時は Firebase Authentication 等を推奨。

---

## 7. 今後の課題・拡張予定

### 7.1 ルーブリック対応

| 項目 | 状態 | 詳細 |
|---|---|---|
| ②⑧を参考値として UI 上で明示 | ✅ v1.4 実装済み | 全画面でグレーアウト・「参考値」バッジ・チャート注記を表示 |
| `AXES` に `evalText`・`ref` フラグを追加 | ✅ v1.4 実装済み | 各軸の「評価すること」テキストと参考値フラグをコードに組み込み |
| `LEVELS` に 0626ルーブリック定義文を追加 | ✅ v1.4 実装済み | `def` フィールドで正式定義を保持（UI への反映は今後） |
| `survey_questions.json` を 0626ルーブリックに完全対応 | 🔲 未対応 | 各軸のLv1〜4定義文を選択肢テキストに反映 |
| 佐藤教授論点③への対応 | 🔲 未対応 | Lv4（創造性）の評価方法を他レベルと分けて設計する可能性 |

### 7.2 未対応機能（優先度順）

| 項目 | 優先度 | 備考 |
|---|---|---|
| `survey_questions.json` の整備 | 高 | ファイルは存在するが、0626ルーブリックの定義文との完全対応は未対応 |
| 認証（パスワード / OAuth） | 高 | Firebase Auth 等の導入 |
| `localStorage` → クラウド DB 移行 | 高 | Firebase / Supabase を想定 |
| ②⑧参考値の UI 表示 | ✅ v1.4 実装済み | — |
| 自己評価 × メンター評価のギャップ強調表示 | 中 | 差分ハイライト・コメント表示 |
| AI 分析機能（STAGE 2） | 中 | 目標と振り返りの整合分析、Lv 候補推定 |
| データエクスポート | 中 | CSV / JSON 出力 |
| PC 向けサイドバーレイアウト | 低 | 元バージョンの UI を取り込む |

### 7.3 AI 評価ロードマップ

| ステージ | 内容 | 現在地 |
|---|---|---|
| STAGE 1 | AI と人で評価（並走） | ← **現在** |
| STAGE 2 | AI がデータ集約・分析 → 人が最終判定 | 次フェーズ（job-analyzer との連携） |
| STAGE 3 | AI が評価を自律判定 → 人は監督 | 将来構想 |

---

## 8. 関連システム

### 8.1 求人分析システム（job-analyzer）

マイナビ求人票を Be-Ready 9軸でスコアリングするシステム。本 POC アプリとは独立したリポジトリ。

| 項目 | 内容 |
|---|---|
| GitHub | `https://github.com/KNJ95/Analyze_Company.git` |
| Vercel | `https://analyze-company-ochre.vercel.app/` |
| ローカルパス | `../job-analyzer/` |

**現行の課題と改善方針（`引き継ぎ資料_求人分析システム_v2_1.md` §4, 追補参照）**:
- 現行：プロンプトに軸名一語のみ → Haiku が語感でキーワードマッチ（精度低）
- 改善：0626ルーブリック全文（B節）をプロンプトに埋め込む → 4レベル定義で採点
- スコア基準変更：「重要度1〜5」→「到達レベルLv1〜4」
- `evidence`（根拠引用）・`confidence` フィールドを追加
- モデルを Haiku → Sonnet に変更予定

### 8.2 関連資料

| 資料 | 内容 |
|---|---|
| `be_ready_project_handover.md` | POC アプリ引き継ぎ資料（打合せ履歴・技術メモ） |
| `引き継ぎ資料_求人分析システム_v2_1.md` | job-analyzer 引き継ぎ資料（0626ルーブリック全文を含む） |
| `be_ready_poc_app.jsx` | 元バージョン（Tailwind + window.storage 構成・参照用） |
| `20260701_佐藤先生訪問_最新版.pptx` | 最新スライド（29ページ・200社分析結果・妥当性検証含む） |
| `0626ルーブリック案.xlsx` | 佐藤教授入力の評価ルーブリック正本 |

---

## 9. 動作環境

| 項目 | 推奨 |
|---|---|
| ブラウザ | Chrome / Safari（iOS Safari 含む） |
| デバイス | PC / スマートフォン |
| 画面サイズ | 360px〜 |
| Node.js | 14 以上（ビルド時） |
| ネット接続 | 初回ロード時のみ必須 |

---

**最終更新日**: 2026年7月1日（v1.4）  
**開発体制**: 2名（谷川・上司）  
**対応プロジェクト**: Be-Ready人材育成プログラム 評価軸 POC（北海学園大学 佐藤教授 共同検討）
